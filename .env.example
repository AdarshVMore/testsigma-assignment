# Optional. Not required to run anything in this repo's V1.
#
# The self-healing locator design leaves a seam for an OpenRouter-backed LLM
# tiebreaker on AMBIGUOUS heals (see Healer's status: 'ambiguous' branch), but
# that fallback is NOT implemented in this V1 (see README "What was left out").
# The key is documented here for completeness / future work only.
OPENROUTER_API_KEY=
